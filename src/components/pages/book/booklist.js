/* eslint-disable no-use-before-define */
import React from 'react';
import MainContainer from '../../container/maincontent';

const BookListPage = () => (
  <MainContainer />
);

export default BookListPage;
